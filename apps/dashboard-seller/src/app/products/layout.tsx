export const metadata = {
  title: 'Meus Produtos - Painel Vendedor | Code & Agent Shop',
  description: 'Lista dos seus produtos: rascunhos, em QA, aprovados, vendas e edicao.',
  robots: { index: false, follow: false },
};

export default function ProductsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
