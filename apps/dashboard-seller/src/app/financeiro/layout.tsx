export const metadata = {
  title: 'Financeiro - Painel Vendedor | Code & Agent Shop',
  description: 'Saldo, payouts, splits Asaas e historico financeiro de vendas.',
  robots: { index: false, follow: false },
};

export default function FinanceiroLayout({ children }: { children: React.ReactNode }) {
  return children;
}
