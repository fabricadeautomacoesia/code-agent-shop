export const metadata = {
  title: 'Novo Produto - Painel Vendedor | Code & Agent Shop',
  description: 'Cadastro de novo produto (Agente IA, Workflow n8n, Script, Template) com upload e QA automatizado.',
  robots: { index: false, follow: false },
};

export default function UploadLayout({ children }: { children: React.ReactNode }) {
  return children;
}
