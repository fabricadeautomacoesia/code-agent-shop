export const metadata = {
  title: 'Perguntas & Respostas - Painel Vendedor | Code & Agent Shop',
  description: 'Perguntas dos compradores sobre seus produtos. Responda em ate 24h para preservar reputacao.',
  robots: { index: false, follow: false },
};

export default function QnaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
