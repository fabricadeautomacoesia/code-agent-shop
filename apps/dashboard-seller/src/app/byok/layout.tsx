export const metadata = {
  title: 'BYOK API Keys - Seller | Code & Agent Shop',
  description: 'Gerencie suas chaves de API (OpenAI, Anthropic, Gemini, etc) para produtos AI agents.',
  robots: { index: false, follow: false, nocache: true },
};

export default function BYOKLayout({ children }: { children: React.ReactNode }) {
  return children;
}
