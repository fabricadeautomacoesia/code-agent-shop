export const metadata = {
  title: 'Minha Loja & KYC - Painel Vendedor | Code & Agent Shop',
  description: 'Configuracao da loja, dados KYC, slug publico, redes sociais e bio.',
  robots: { index: false, follow: false },
};

export default function LojaLayout({ children }: { children: React.ReactNode }) {
  return children;
}
