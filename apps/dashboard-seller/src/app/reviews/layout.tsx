export const metadata = {
  title: 'Avaliacoes - Painel Vendedor | Code & Agent Shop',
  description: 'Avaliacoes recebidas pelos compradores. Responda criticas e proteja sua reputacao.',
  robots: { index: false, follow: false },
};

export default function ReviewsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
