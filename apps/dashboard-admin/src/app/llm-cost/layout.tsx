export const metadata = {
  title: 'LLM Cost - Admin | Code & Agent Shop',
  description: 'Observabilidade de custos LLM (OpenAI/Gemini/Groq) por provider/dia.',
  robots: { index: false, follow: false },
};

export default function LlmCostLayout({ children }: { children: React.ReactNode }) {
  return children;
}
