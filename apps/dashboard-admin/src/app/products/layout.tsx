export const metadata = {
  title: 'Produtos - Admin | Code & Agent Shop',
  description: 'Lista geral de produtos: status, vendas, force-approve, platform-take.',
  robots: { index: false, follow: false },
};

export default function ProductsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
