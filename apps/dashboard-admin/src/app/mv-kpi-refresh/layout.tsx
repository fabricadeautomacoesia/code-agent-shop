export const metadata = {
  title: 'MV KPI Refresh - Admin | Code & Agent Shop',
  description: 'Manual refresh do mv_seller_kpi materialized view + audit log historico.',
  robots: { index: false, follow: false },
};

export default function MvKpiRefreshLayout({ children }: { children: React.ReactNode }) {
  return children;
}
