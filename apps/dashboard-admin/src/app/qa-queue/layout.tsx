export const metadata = {
  title: 'QA Queue - Admin | Code & Agent Shop',
  description: 'Fila de produtos aguardando aprovacao LLM. Force-approve, rejeicao manual.',
  robots: { index: false, follow: false },
};

export default function QaQueueLayout({ children }: { children: React.ReactNode }) {
  return children;
}
